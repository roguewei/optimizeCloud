#spring:
#  cloud:
#    config:
#      # 需要从GitHub上读取的资源名称，注意没有yml后缀名
#      name: microservicecloud-config-client
#      # 本次访问的配置项
#      profile: test
#      label: master
#      # 本微服务启动后先去找3344的服务，通过springcloudconfig获取GitHub的服务地址
#      uri: http://config-3344.com:3344
spring:
  cloud:
    config:
      name: optimizecloud-config-provider-client #需要从github上读取的资源文件名称，注意没有yml后缀名
      profile: dev1   #本次访问的配置项
      label: master
      uri: http://127.0.0.1:1001  #本微服务启动后先去找3344号服务，通过SpringCloudConfig获取GitHub的服务地址